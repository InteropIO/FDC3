{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://fdc3.finos.org/schemas/next/api/intentResult.schema.json",
    "title": "IntentResult",
    "description": "Describes results that an Intent handler may optionally return that should be communicated back to the app that raised the intent, via the IntentResolution.",
    "type": "object",
    "oneOf": [
        {
            "$ref": "../context/context.schema.json"
        },
        {
            "$ref": "channel.schema.json"
        }
    ]
}